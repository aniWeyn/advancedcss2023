<template>
  <div class="slide">
    <hr />
    <h3></h3>
    <section>
      <a
        target="_blank"
        href="https://developer.mozilla.org/en-US/docs/Web/CSS/:nth-of-type"
        ><code>:nth-of-type</code></a
      >
      <p>
        Uses An+B notation to select elements from a list of sibling elements
        that match a certain type from a list of sibling elements.
      </p>
    </section>
    <section>
      <a
        target="_blank"
        href="https://developer.mozilla.org/en-US/docs/Web/CSS/:nth-last-of-type"
        ><code>:nth-last-of-type</code></a
      >
      <p>
        Uses An+B notation to select elements from a list of sibling elements
        that match a certain type from a list of sibling elements counting
        backwards from the end of the list.
      </p>
    </section>
    <section>
      <a
        target="_blank"
        href="https://developer.mozilla.org/en-US/docs/Web/CSS/:first-of-type"
        ><code>:first-of-type</code></a
      >
      <p>
        Matches an element that is the first of its siblings, and also matches a
        certain type selector.
      </p>
    </section>
    <section>
      <a
        target="_blank"
        href="https://developer.mozilla.org/en-US/docs/Web/CSS/:last-of-type"
        ><code>:last-of-type</code></a
      >
      <p>
        Matches an element that is the last of its siblings, and also matches a
        certain type selector.
      </p>
    </section>
    <section>
      <a
        target="_blank"
        href="https://developer.mozilla.org/en-US/docs/Web/CSS/:only-of-type"
        ><code>:only-of-type</code></a
      >
      <p>
        Matches an element that has no siblings of the chosen type selector.
      </p>
    </section>
  </div>
</template>
